{
  "name": "community-cafe-e2e",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "End-to-end (Playwright) tests for Community Cafe Web, driving both public sites + the admin against the e2e docker stack.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "typecheck": "tsc --noEmit",
    "report": "playwright show-report",
    "stack:up": "docker compose -f ../docker-compose.e2e.yml up --build -d",
    "stack:down": "docker compose -f ../docker-compose.e2e.yml down -v"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "typescript": "^6.0.3",
    "@types/node": "^26.0.0"
  }
}
