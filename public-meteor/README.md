# public-meteor

Vite + React SPA for **meteor.cafe**, Meteor's own design and identity (incl. the open/closed
status banner). Renders the shared CMS content plus Meteor's hardcoded page copy.

_Scaffolded in a later step (Vite + React + TS + Tailwind, consuming `shared-web`)._
