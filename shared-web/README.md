# shared-web

Shared low-level building blocks consumed by both public sites and the admin:
API client (`fetchWithRetry`), TypeScript types, theme tokens (Hubble + Meteor palettes,
self-hosted AXIS/Lato fonts), and shared build/lint config.

**Not** page layouts or page components. The two public sites diverge by design.

_Scaffolded in a later step._
