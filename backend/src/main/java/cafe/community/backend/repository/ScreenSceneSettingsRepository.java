package cafe.community.backend.repository;

import cafe.community.backend.model.ScreenSceneSettings;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ScreenSceneSettingsRepository extends JpaRepository<ScreenSceneSettings, Long> {
}
